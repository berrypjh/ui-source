import React from 'react';
import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface ReactNativeUiProps {}

export function ReactNativeUi(props: ReactNativeUiProps) {
  return (
    <View>
      <Text>Welcome to react-native-ui!</Text>
    </View>
  );
}

export default ReactNativeUi;
