{
  "name": "@berrypjh/design-tokens",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/design-tokens/src",
  "projectType": "library",
  "targets": {
    "build:tokens": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx libs/design-tokens/src/build.ts"
      }
    },
    "build:ts": {
      "executor": "nx:run-commands",
      "dependsOn": ["build:tokens"],
      "options": {
        "command": "tsc -p libs/design-tokens/tsconfig.lib.json"
      }
    },
    "build": {
      "executor": "nx:noop",
      "dependsOn": ["build:tokens", "build:ts"]
    },
    "publish": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "cd libs/design-tokens && pnpm publish --registry=https://npm.pkg.github.com"
      }
    },
    "nx-release-publish": {
      "executor": "@nx/js:release-publish",
      "dependsOn": ["build"]
    }
  },
  "tags": [],
  "release": {
    "version": {
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  }
}
